<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SAHAB × Cliptx — عرض تسويقي استراتيجي</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800;900&display=swap');
:root{
    --navy:#0A003F;
    --blue:#2701FB;
    --blue-light:#5b3dff;
    --bg:#F6F6FB;
    --card:#FFFFFF;
    --ink:#0A003F;
    --muted:#5b5b76;
    --line:#E5E3F5;
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    font-family:'Tajawal', sans-serif;
    background:var(--bg);
    color:var(--ink);
    line-height:1.9;
  }
  h1,h2,h3,h4{font-family:'Tajawal', sans-serif; font-weight:900;}

  /* ---------- TOP BAR ---------- */
  .topbar{
    position:sticky; top:0; z-index:100;
    background:rgba(10,0,63,0.96);
    backdrop-filter:blur(6px);
    color:#fff;
    display:flex; align-items:center; justify-content:space-between;
    padding:14px 6%;
    border-bottom:1px solid rgba(255,255,255,0.08);
  }
  .topbar .brand{display:flex; align-items:center; gap:10px; font-family:'Tajawal'; font-weight:800; font-size:15px;}
  .topbar .brand span{color:#a89bff;}
  .topbar nav{display:flex; gap:22px; overflow-x:auto;}
  .topbar nav a{color:#cfc9ff; text-decoration:none; font-size:13px; font-weight:500; white-space:nowrap;}
  .topbar nav a:hover{color:#fff;}

  /* ---------- HERO ---------- */
  .hero{
    background:radial-gradient(circle at 30% 20%, #1c0d7a 0%, var(--navy) 55%, #05001f 100%);
    color:#fff;
    padding:110px 6% 90px;
    text-align:center;
    position:relative;
    overflow:hidden;
  }
  .hero::before{
    content:"";
    position:absolute; inset:0;
    background-image:radial-gradient(1px 1px at 20% 30%, rgba(255,255,255,.25) 1px, transparent 1px),
                      radial-gradient(1px 1px at 70% 60%, rgba(255,255,255,.2) 1px, transparent 1px),
                      radial-gradient(1px 1px at 40% 80%, rgba(255,255,255,.15) 1px, transparent 1px);
    background-size:300px 300px;
  }
  .hero .tag{
    display:inline-block; background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.2);
    padding:7px 18px; border-radius:30px; font-size:13px; letter-spacing:.5px; margin-bottom:26px; color:#cfc9ff;
  }
  .hero h1{
    font-size:44px; font-weight:900; margin-bottom:14px; position:relative;
    background:linear-gradient(90deg,#fff,#c9c1ff);
    -webkit-background-clip:text; background-clip:text; color:transparent;
  }
  .hero p.sub{font-size:18px; color:#c9c4f0; max-width:680px; margin:0 auto 34px; position:relative;}
  .hero .meta{display:flex; justify-content:center; gap:34px; flex-wrap:wrap; position:relative;}
  .hero .meta div{font-size:13px; color:#a89bff;}
  .hero .meta b{display:block; color:#fff; font-size:16px; font-family:'Tajawal'; font-weight:700; margin-top:2px;}

  /* ---------- SECTION SHELL ---------- */
  section{padding:70px 6%; max-width:1180px; margin:0 auto;}
  section.alt{background:transparent;}
  .section-head{display:flex; align-items:center; gap:16px; margin-bottom:38px;}
  .num-badge{
    width:52px; height:52px; min-width:52px; border-radius:16px;
    background:linear-gradient(135deg, var(--blue), var(--blue-light));
    color:#fff; display:flex; align-items:center; justify-content:center;
    font-family:'Tajawal'; font-weight:800; font-size:18px;
    box-shadow:0 8px 20px rgba(39,1,251,0.25);
  }
  .section-head h2{font-size:28px; font-weight:800; color:var(--navy);}
  .section-head p{color:var(--muted); font-size:14px; margin-top:2px;}

  .lead{font-size:16px; color:#33325b; max-width:920px; margin-bottom:30px;}
  .callout{
    background:#EFECFF; border-right:4px solid var(--blue); border-radius:10px;
    padding:18px 22px; font-size:14.5px; color:var(--navy); margin:26px 0;
  }
  .callout b{color:var(--blue);}

  /* ---------- CARD GRID ---------- */
  .grid{display:grid; gap:18px;}
  .grid.cols-2{grid-template-columns:repeat(2, 1fr);}
  .grid.cols-3{grid-template-columns:repeat(3, 1fr);}
  .grid.cols-4{grid-template-columns:repeat(4, 1fr);}
  @media(max-width:900px){
    .grid.cols-2, .grid.cols-3, .grid.cols-4{grid-template-columns:1fr;}
    .hero h1{font-size:30px;}
    section{padding:50px 6%;}
  }
  .card{
    background:var(--card); border:1px solid var(--line); border-radius:16px; padding:22px;
    transition:transform .2s, box-shadow .2s;
  }
  .card:hover{transform:translateY(-3px); box-shadow:0 14px 30px rgba(10,0,63,0.08);}
  .card h4{color:var(--navy); font-size:16px; font-weight:700; margin-bottom:8px; display:flex; align-items:center; gap:8px;}
  .card h4 .dot{width:8px; height:8px; border-radius:50%; background:var(--blue); display:inline-block;}
  .card p{font-size:13.5px; color:var(--muted);}

  /* KPI style cards with 4 rows */
  .kpi-card{background:var(--card); border:1px solid var(--line); border-radius:16px; overflow:hidden;}
  .kpi-card .kpi-top{background:linear-gradient(135deg, var(--navy), var(--blue)); color:#fff; padding:14px 20px; font-family:'Tajawal'; font-weight:700; font-size:15px;}
  .kpi-card .kpi-body{padding:16px 20px;}
  .kpi-row{padding:10px 0; border-bottom:1px dashed var(--line);}
  .kpi-row:last-child{border-bottom:none;}
  .kpi-row .k{font-size:12px; color:var(--blue); font-weight:700; font-family:'Tajawal'; text-transform:uppercase; letter-spacing:.3px;}
  .kpi-row .v{font-size:13.5px; color:var(--navy); margin-top:3px;}
  .kpi-row .w{font-size:12.5px; color:var(--muted); margin-top:3px;}

  /* ---------- TIMELINE (funnel stages) ---------- */
  .funnel{display:flex; flex-direction:column; gap:0; margin:20px 0;}
  .funnel-step{
    display:grid; grid-template-columns:110px 1fr; gap:20px; align-items:stretch;
    position:relative; padding-bottom:26px;
  }
  .funnel-step:not(:last-child)::before{
    content:""; position:absolute; right:54px; top:46px; bottom:-6px; width:2px;
    background:repeating-linear-gradient(to bottom, var(--line) 0 6px, transparent 6px 12px);
  }
  .funnel-num{
    width:44px; height:44px; border-radius:50%; background:#fff; border:2px solid var(--blue); color:var(--blue);
    display:flex; align-items:center; justify-content:center; font-family:'Tajawal'; font-weight:800; margin-inline-start:32px;
    z-index:2;
  }
  .funnel-content{background:var(--card); border:1px solid var(--line); border-radius:14px; padding:16px 20px;}
  .funnel-content h4{color:var(--navy); font-family:'Tajawal'; font-weight:700; font-size:15px; margin-bottom:4px;}
  .funnel-content .when{color:var(--blue); font-size:12px; font-weight:700; margin-bottom:6px;}
  .funnel-content p{font-size:13px; color:var(--muted);}

  /* ---------- 90 day roadmap ---------- */
  .phases{display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin:24px 0;}
  @media(max-width:900px){.phases{grid-template-columns:1fr;}}
  .phase{background:var(--card); border:1px solid var(--line); border-radius:16px; padding:20px; position: